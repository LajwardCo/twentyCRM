import { type SurveyForm } from '../../../api/surveys';

// Placeholder — replaced by the surveys UI build.
export const FormSharePanel = (_props: { form: SurveyForm; onChanged: () => void }) => null;
