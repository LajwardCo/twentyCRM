import { type CurrentUser } from '../../api/auth';

// Placeholder — replaced by the surveys UI build.
export const CampaignDetailView = (_props: { campaignId: string; user: CurrentUser }) => (
  <main className="page">
    <div className="page-head">
      <h1>کمپاین</h1>
    </div>
  </main>
);
