// Placeholder — replaced by the surveys UI build.
export const FormPreviewView = (_props: { formId: string; query: string }) => (
  <main className="page">
    <div className="page-head">
      <h1>پیش‌نمایش</h1>
    </div>
  </main>
);
