// Placeholder — replaced by the surveys UI build.
export const FormPrintView = (_props: { formId: string; query: string }) => (
  <main className="page">
    <div className="page-head">
      <h1>چاپ فرم</h1>
    </div>
  </main>
);
