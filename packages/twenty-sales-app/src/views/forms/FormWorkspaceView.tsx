import { type CurrentUser } from '../../api/auth';

// Placeholder — replaced by the surveys UI build.
export const FormWorkspaceView = (_props: { formId: string; tab: string; user: CurrentUser }) => (
  <main className="page">
    <div className="page-head">
      <h1>کارگاه فرم</h1>
    </div>
  </main>
);
