import { type CurrentUser } from '../../api/auth';

// Placeholder — replaced by the surveys UI build.
export const PaperEntryView = (_props: { formId: string | null; query: string; user: CurrentUser }) => (
  <main className="page">
    <div className="page-head">
      <h1>ورود پاسخ کاغذی</h1>
    </div>
  </main>
);
