// Placeholder — replaced by the surveys UI build.
export const PublicFormView = (_props: { slug: string; query: string }) => (
  <main className="page">
    <div className="page-head">
      <h1>فرم</h1>
    </div>
  </main>
);
