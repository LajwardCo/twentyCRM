import { type CurrentUser } from '../../api/auth';

// Placeholder — replaced by the surveys UI build.
export const ResponseDetailView = (_props: { responseId: string; user: CurrentUser }) => (
  <main className="page">
    <div className="page-head">
      <h1>جزئیات پاسخ</h1>
    </div>
  </main>
);
