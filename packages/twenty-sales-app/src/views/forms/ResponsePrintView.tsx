// Placeholder — replaced by the surveys UI build.
export const ResponsePrintView = (_props: { responseId: string }) => (
  <main className="page">
    <div className="page-head">
      <h1>چاپ پاسخ</h1>
    </div>
  </main>
);
