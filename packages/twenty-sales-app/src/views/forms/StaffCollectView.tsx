import { type CurrentUser } from '../../api/auth';

// Placeholder — replaced by the surveys UI build.
export const StaffCollectView = (_props: { formId: string; query: string; user: CurrentUser }) => (
  <main className="page">
    <div className="page-head">
      <h1>جمع‌آوری حضوری</h1>
    </div>
  </main>
);
