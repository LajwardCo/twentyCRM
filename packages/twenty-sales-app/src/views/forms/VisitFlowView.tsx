import { type CurrentUser } from '../../api/auth';

// Placeholder — replaced by the surveys UI build.
export const VisitFlowView = (_props: { query: string; user: CurrentUser }) => (
  <main className="page">
    <div className="page-head">
      <h1>ثبت بازدید</h1>
    </div>
  </main>
);
